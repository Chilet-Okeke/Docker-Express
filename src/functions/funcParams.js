// Parametres are placeholders used when declaring a function
// local vars and args

// Use params inside the function to store values

let titled, myName, myAge;
titled = "Sir",
myName = 'Chilet',
myAge = 25

function greet(title) {
    console.log("Good morning " + title)
}

// pass arg
greet('sir')
greet(myName)